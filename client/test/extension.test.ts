import * as assert from 'assert';

import * as vscode from 'vscode';
import {SpecflowDriver} from '../src/driver/SpecflowDriver';

describe('SpecflowDriver', () => {
  describe('suggestBindings()', () => {
    it('testcase1', () => {
      
      // var specflowDriver = new SpecflowDriver(new );
      // var bindings = specflowDriver.suggestBindings();
      // assert.equal(bindings, null);
    });
    
  });
});